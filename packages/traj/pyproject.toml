[project]
name = "aau-ais-traj"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [{ name = "Kasper Fromm Pedersen", email = "kasperf@cs.aau.dk" }]
requires-python = ">=3.12"
dependencies = [
    "adbc-driver-gizmosql==1.1.*",
    "jinja2==3.*",
    "aau-ais-schema",
]

[tool.uv.sources]
aau-ais-schema = { path = '../schema', editable = true }
aau-ais-core = { path = '../core', editable = true }

[dependency-groups]
dev = ["pyarrow-stubs==20.0.*"]


[project.scripts]
lakehouse = "aau_traj.main:app"

[build-system]
requires = ["uv_build>=0.11.6,<0.12.0"]
build-backend = "uv_build"
